/**
 * Centre d’aide — liens hors API (Figma `1586:1194`).
 * WhatsApp : numéro mock jusqu’à branchement `settings.site`.
 * Doc : `docs/reglages-contact-mocks.md`.
 */
export const centreAideMock = {
  /** Lien WhatsApp `wa.me` (indicatif + numéro, sans +). */
  whatsappPhone: '33700000000',
} as const
